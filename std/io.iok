fn print(str) => {
  let output = ""

  for part -> str.split("{") {
    let bits = part.split("}")
    if bits.len() == 2 {
      output += __get_var_from_str(bits[0]) + bits[1]
    } els {
      output += part
    }
  }

  write(output)
}

fn println(str) => print(str+"\n")