import "io.iok" @ io
import "fs.iok" @ fs
import "net.iok" @ net
import "ffi.iok" @ ffi
import "math.iok" @ math
import "time.iok" @ time
import "env.iok" @ env

fn assert(cond, msg) => {
  if !cond {
    write("Assert failed: " + msg + "\n")
    exit(1)
  }
}
